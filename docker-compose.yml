services:
  devths-be-1:
    image: ${FULL_IMAGE}
    container_name: devths-be-1
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '8080:8080'
    volumes:
      - ${LOG_FILE_DIRECTORY}/be-1:/app/log
    networks:
      - devths-network
    healthcheck:
      test: [ 'CMD', 'curl', '-f', 'http://localhost:8080/actuator/health' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  devths-be-2:
    image: ${FULL_IMAGE}
    container_name: devths-be-2
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - SERVER_PORT=8081
    ports:
      - '8081:8081'
    volumes:
      - ${LOG_FILE_DIRECTORY}/be-2:/app/log
    networks:
      - devths-network
    healthcheck:
      test: [ 'CMD', 'curl', '-f', 'http://localhost:8081/actuator/health' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  promtail:
    image: grafana/promtail:latest
    container_name: devths-be-promtail
    restart: unless-stopped
    environment:
      - LOKI_URL=${LOKI_URL}
      - ENV=${ENV}
    volumes:
      - ./promtail-config.yml:/etc/promtail/config.yml:ro
      - ${LOG_FILE_DIRECTORY}:/app/log:ro
    command: -config.file=/etc/promtail/config.yml --config.expand-env=true
    networks:
      - devths-network
    healthcheck:
      test: [ 'CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:9080/ready' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: 'json-file'
      options:
        max-size: '5m'
        max-file: '2'

networks:
  devths-network:
    driver: bridge
